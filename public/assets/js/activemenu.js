$(document).ready(function(){
	
	var url = window.location.pathname.substring(1);

	//si cambia el nombre de la clase pero no funciona
	/*if(url == 'dashboard')
		$('a.prueba').attr('class', 'active');*/
});